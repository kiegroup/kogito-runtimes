<?xml version="1.0" encoding="UTF-8"?>
<!--
  Maven settings used by the nightly quarkus-openapi-generator SNAPSHOT integration workflow.
  Activates the Maven Central Snapshots repository so that SNAPSHOT versions of
  io.quarkiverse.openapi.generator:quarkus-openapi-generator (and its transitive
  SNAPSHOT dependencies) are resolved automatically without modifying any POM.

  Also carries credentials for the "github" server id (GitHub Packages), matching
  the repository declared in the project POM. The mvn "settings" flag on the
  command line replaces the settings.xml that actions/setup-java generates,
  which would otherwise drop those credentials.
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0
                              https://maven.apache.org/xsd/settings-1.2.0.xsd">

  <servers>
    <server>
      <id>github</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>

  <profiles>
    <profile>
      <id>central-snapshots</id>
      <repositories>
        <repository>
          <id>central-snapshots</id>
          <url>https://central.sonatype.com/repository/maven-snapshots/</url>
          <releases>
            <enabled>false</enabled>
          </releases>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </repository>
      </repositories>
      <pluginRepositories>
        <pluginRepository>
          <id>central-snapshots</id>
          <url>https://central.sonatype.com/repository/maven-snapshots/</url>
          <releases>
            <enabled>false</enabled>
          </releases>
          <snapshots>
            <enabled>true</enabled>
          </snapshots>
        </pluginRepository>
      </pluginRepositories>
    </profile>
  </profiles>

  <activeProfiles>
    <activeProfile>central-snapshots</activeProfile>
  </activeProfiles>

</settings>
